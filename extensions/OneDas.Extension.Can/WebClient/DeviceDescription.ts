class DeviceDescription
{
    public Name: string
    public HardwareId: string

    constructor(name: string, hardwareId: string)
    {
        this.Name = name
        this.HardwareId = hardwareId
    }
}